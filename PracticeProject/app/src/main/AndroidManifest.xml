<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nanioi.practiceproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PracticeProject"
        android:usesCleartextTraffic="true">

        <activity android:name=".PrecautionActivity"
            android:theme="@style/Theme.PracticeProject.NoActionBar"></activity>
        <activity android:name=".NewActivity"
            android:theme="@style/Theme.PracticeProject.NoActionBar"/>
        <activity android:name=".AccumulateActivity"
            android:theme="@style/Theme.PracticeProject.NoActionBar"/>
        <activity android:name=".SummaryActivity"
            android:theme="@style/Theme.PracticeProject.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:theme="@style/Theme.PracticeProject.NoActionBar">
        </activity>
    </application>

</manifest>
